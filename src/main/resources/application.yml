spring:
  config:
    activate:
      on-profile: test
server:
  port: 4000
welcomeMsg: Welcome from Test
---
spring:
  config:
    activate:
      on-profile: prod
server:
  port: 5000
welcomeMsg: Welcome from Prod
